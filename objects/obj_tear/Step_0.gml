/// @description Act with creator

// Moving along with the creator
x += creator.hspd;
y += creator.vspd;

// Changing the alpha
image_alpha = creator.image_alpha;

// Flipping the sprite in the right direction
image_xscale = creator.image_xscale;

